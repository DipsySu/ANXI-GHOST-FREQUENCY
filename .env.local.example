# Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: Use official Google AI SDK instead of REST API
# Set to 'true' to use SDK (recommended for direct Google API access)
# Set to 'false' to use REST API with fetch (required when using BASE_URL proxy)
USE_GEMINI_SDK=true

# Optional: Custom API endpoint (for proxy/relay services)
# When set, USE_GEMINI_SDK will be automatically forced to false
# BASE_URL=http://your-proxy-endpoint:port

# Optional: Custom model names
# GEMINI_TEXT_MODEL=gemini-2.0-flash-exp
# GEMINI_IMAGE_MODEL=gemini-3-pro-image

# Enable/disable AI image generation (true/false)
# Set to false to skip image generation and save API costs
ENABLE_IMAGE_GENERATION=true
